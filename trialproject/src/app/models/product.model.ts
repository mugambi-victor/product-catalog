// product.model.ts

export interface Product {
    id: number;
    name: string;
    description: string;
    price: number;
    // Add other properties as needed based on your Laravel Product model
  }
